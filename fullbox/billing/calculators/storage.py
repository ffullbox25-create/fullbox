from __future__ import annotations


def calculate(application) -> list:
    # Хранение считается отдельным дневным job-ом record_storage_billing.
    # В общем пересчёте не дублируем дневные строки.
    return []
